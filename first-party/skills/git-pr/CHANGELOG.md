# Changelog

## 1.0.0

- Initial release under `emploke/git-pr`. Migrated from `langsensei/git-pr` in the community marketplace and relocated into the emploke first-party catalog because the skill depends on emploke's `$EMPLOKE_WORKSPACE_DIR` runtime contract.
